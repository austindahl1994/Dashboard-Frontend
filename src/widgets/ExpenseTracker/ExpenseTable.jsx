import PropTypes from 'prop-types'

const ExpenseTable = ({categories, subcategories}) => {
  
  return (
    <div>ExpenseTable</div>
  )
}

ExpenseTable.propTypes = {
    categories: PropTypes.array,
    subcategories: PropTypes.array
}

export default ExpenseTable