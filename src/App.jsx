import CharacterGeneration from "./widgets/CharGen/CharacterGeneration";

function App() {
  //create a state that saves the name and properties of profile
  //create a frontend component that calls CharacterGeneration component, but 
  //returns the component, holds the object to be returned with name?

  return (
    <>
      <CharacterGeneration />
    </>
  );
}

export default App;
